spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Gyrmaniya
    username: postgres
    password: Banka!1235
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true